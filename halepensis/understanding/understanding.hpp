//#pragma once
//
//class TaskUnderstanding;
//class Entity;
//
//auto detect_objects(TaskUnderstanding& task) -> void;
///// Detects which objects are involved in the task and fills in focus_descriptors in the task.
//auto detect_change(TaskUnderstanding& task) -> void;
//
//auto detect_features(Entity& object) -> void;
